import {
    listarProductos
} from "../productos/controller/productos.controller.js";

import {
    saveVentas, 
    listarVentas
} from "./ventas.controller.js";

import {
    listarDetalle
} from "../detalles/controller/detalle.controller.js";

export {
    listarProductos,
    saveVentas,
    listarVentas,
    listarDetalle
}