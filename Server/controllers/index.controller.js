import {
    listarProductos
} from "./productos.controller.js";

import {
    saveVentas, 
    listarVentas
} from "./ventas.controller.js";

import {
    listarDetalle
} from "./detalle.controller.js";

export {
    listarProductos,
    saveVentas,
    listarVentas,
    listarDetalle
}